"""The ibuilder/massing adoption kit. See README.md in this directory.

These modules are written against massing's `aec_api` package and are *copied
there* by `scripts/vendor_to_massing.py`. They import only `massingplan.core`
and each other, so they type-check and lint here, where they are maintained.
"""
